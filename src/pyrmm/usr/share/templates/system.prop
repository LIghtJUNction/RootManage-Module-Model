# 系统属性设置文件
# 格式：[key]=[value]
# 这些属性将在系统启动时通过 resetprop 设置

# 示例属性设置：
# ro.example.property=value
# persist.example.setting=1

# 注意：某些属性可能需要在特定的启动阶段设置
# 如果需要在 post-fs-data 阶段设置，请在相应脚本中使用 resetprop -n
